'use strict';

const { assert } = require('kixx-assert');
const sinon = require('sinon');

const {
	Logger,
	createLogger,
	streams,
	serializers } = require('../');

module.exports = (test) => {

	function noop() {
	}

};
